---
ansible-navigator:
  execution-environment:
    enabled: false
    # enabled: true
    image: quay.io/ansible/network-ee:latest
    pull:
      policy: missing
  mode: stdout
  ansible:
    inventory:
      help: false
      entries:
        - inventory
  playbook-artifact:
    enable: false
